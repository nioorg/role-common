---
apt:
  tools:
    - vim
    - ufw

timezone: Europe/Rome

# admin_accounts:
#   - username: admin
#     password: password
#     ssh:
#       key_file: ~/.ssh/admin_key_file
#       passphrase: password

# user_accounts:
#   - username: user
#     password: password
#     ssh:
#       key_file: ~/.ssh/user_key_file
#       passphrase: password

# firewall_ipv6: "yes"
firewall:
  default_incoming: deny
  default_outgoing: allow
